# 로컬 LLM 기반 이메일 생성기 — 전체 학습 기록
**정리일:** 2026-07-03  
**학습 목적:** 로컬 AI 모델을 활용한 웹 앱 개발 전 과정 이해  
**프로젝트 경로:** `C:\Users\Jini Nam\Documents\GitHub\Multi lang email generator\`

---

## 전체 학습 흐름 한눈에 보기

```
LLM 개념 이해
    ↓
HuggingFace에서 Llama 2 모델 다운로드 (GGML 포맷)
    ↓
CTransformers로 로컬 실행 시도
    ↓
Google Colab에서 CPU vs GPU 속도 비교 실험
    ↓
Streamlit으로 웹 UI 만들기 학습
    ↓
LangChain으로 프롬프트 관리 학습
    ↓
Gemini API 연동 실습 (인공지능 시인 앱)
    ↓
Ollama로 더 쉽게 로컬 LLM 실행
    ↓
이메일 생성기 앱 완성
    ↓
오류 해결 및 배포 (바탕화면 바로가기)
```

---

## CHAPTER 1. LLM과 로컬 모델 이해

### LLM(Large Language Model)이란?
- 대량의 텍스트 데이터로 학습된 대형 언어 모델
- ChatGPT, Gemini, Claude 등이 대표적
- **클라우드 LLM**: 인터넷으로 서버에 요청 → 비용 발생, 개인정보 외부 전송
- **로컬 LLM**: 내 컴퓨터에서 직접 실행 → 무료, 개인정보 보호

### Meta의 Llama 모델 시리즈
| 모델 | 특징 |
|------|------|
| Llama 2 (2023) | Meta 오픈소스 공개. 상업적 이용 가능. 7B/13B/70B 파라미터 버전 제공 |
| Llama 3 (2024) | 성능 대폭 향상. 8B/70B 버전. 한국어 성능 개선 |
| Llama 3.1 (2024) | 128K 컨텍스트 지원. 멀티링궐 강화 |

- **7B, 8B, 70B**: 모델 파라미터(가중치) 수. 숫자가 클수록 성능 좋지만 더 많은 RAM/VRAM 필요
- **Chat 버전**: 일반 대화용으로 파인튜닝된 버전 (지시 따르기에 최적화)

---

## CHAPTER 2. 모델 파일 포맷 — GGML과 GGUF

### 파일 포맷이 왜 필요한가?
AI 모델의 학습된 가중치(수십억 개의 숫자)를 파일로 저장하는 방식.  
일반 PyTorch 모델(.pt)은 GPU 없이 실행이 어렵고 무겁기 때문에, **CPU에서도 실행 가능한 경량 포맷**이 개발됨.

### GGML (구버전)
- Georgi Gerganov가 개발한 C 기반 머신러닝 라이브러리의 포맷
- 파일 확장자: `.bin`
- 이 프로젝트에서 다운받은 파일: `llama-2-7b-chat.ggmlv3.q8_0.bin` (7.16 GB)

```
파일명 해석: llama-2-7b-chat . ggmlv3 . q8_0 . bin
              모델명          포맷버전  양자화    확장자
```

### GGUF (신버전, 현재 표준)
- GGML의 후속 포맷. 2023년 하반기부터 표준
- 파일 확장자: `.gguf`
- 메타데이터 포함, 더 안정적
- Google Colab 실험에서 사용: `llama-2-7b-chat.Q4_K_M.gguf`

### 양자화(Quantization)란?
모델 크기를 줄이는 기술. 원래 32비트 소수를 더 작은 비트로 압축.

| 양자화 | 비트 | 파일 크기 | 품질 |
|--------|------|-----------|------|
| Q8_0 | 8비트 | ~7GB | 원본과 거의 동일 |
| Q4_K_M | 4비트 | ~4GB | 약간 손실, 속도 빠름 |
| Q2_K | 2비트 | ~2GB | 품질 낮음, 매우 가벼움 |

> 이 프로젝트의 `.bin` 파일은 Q8_0 양자화 → 품질이 높은 대신 파일이 7GB로 큼

---

## CHAPTER 3. HuggingFace에서 모델 다운로드

### HuggingFace란?
- AI 모델, 데이터셋을 공유하는 플랫폼 (AI계의 GitHub)
- URL: https://huggingface.co
- `TheBloke`라는 유저가 다양한 모델의 GGML/GGUF 버전을 변환해서 업로드한 것으로 유명

### 다운로드한 모델
```
출처: https://huggingface.co/TheBloke/Llama-2-7B-Chat-GGML
파일: llama-2-7b-chat.ggmlv3.q8_0.bin
크기: 7.16 GB
날짜: 2026-06-12
```

### 다운로드 방법 (참고)
```bash
# HuggingFace CLI 사용
pip install huggingface_hub
huggingface-cli download TheBloke/Llama-2-7B-Chat-GGML llama-2-7b-chat.ggmlv3.q8_0.bin

# 또는 브라우저에서 직접 다운로드
```

---

## CHAPTER 4. CTransformers — Python에서 GGML/GGUF 모델 실행

### CTransformers란?
- GGML 모델을 Python에서 실행할 수 있게 해주는 라이브러리
- C/C++로 구현되어 있어 CPU에서도 빠르게 동작
- GPU 가속도 지원 (CUDA 버전 별도 설치)

```bash
pip install ctransformers          # CPU 버전
pip install ctransformers[cuda]    # GPU(CUDA) 버전
```

### 기본 사용법
```python
from ctransformers import AutoModelForCausalLM

# 로컬 파일에서 모델 로드
model = AutoModelForCausalLM.from_pretrained(
    './llama-2-7b-chat.ggmlv3.q8_0.bin',  # 파일 경로
    model_type='llama',                     # 모델 종류
    config={
        'max_new_tokens': 512,   # 최대 생성 토큰 수
        'temperature': 0.01      # 창의성 수치 (낮을수록 일정한 답변)
    }
)

result = model("안녕하세요, 자기소개 부탁드립니다.")
print(result)
```

### LangChain과 CTransformers 연동 (원본 코드 방식)
```python
from langchain_community.llms.ctransformers import CTransformers

llm = CTransformers(
    model='./llama-2-7b-chat.ggmlv3.q8_0.bin',
    model_type='llama',
    config={'max_new_tokens': 512, 'temperature': 0.01}
)

response = llm.invoke("이메일을 작성해주세요.")
```

> **이 프로젝트 원본 코드에 CTransformers import가 있었던 이유:**  
> 처음에는 CTransformers로 로컬 `.bin` 파일을 직접 실행하는 방식으로 개발했다가,  
> 나중에 Ollama 방식으로 전환하면서 해당 코드는 주석 처리됨.

---

## CHAPTER 5. Google Colab GPU 실험 — CPU vs GPU 속도 비교

**파일:** `test_ctransformers_gpu.ipynb`  
**실행 환경:** Google Colab (T4 GPU)

### 실험 목적
같은 모델을 CPU와 GPU에서 실행했을 때 속도 차이가 얼마나 나는지 비교.

### 실험 코드 요약
```python
from ctransformers import AutoModelForCausalLM
import time

# HuggingFace에서 직접 GGUF 모델 로드 (자동 다운로드)
# CPU 모드
model_cpu = AutoModelForCausalLM.from_pretrained(
    "TheBloke/Llama-2-7B-Chat-GGUF",
    model_file="llama-2-7b-chat.Q4_K_M.gguf",
    model_type="llama",
    gpu_layers=0,          # GPU 레이어 0 = 전부 CPU에서 처리
    context_length=512,
)

# GPU 모드
model_gpu = AutoModelForCausalLM.from_pretrained(
    "TheBloke/Llama-2-7B-Chat-GGUF",
    model_file="llama-2-7b-chat.Q4_K_M.gguf",
    model_type="llama",
    gpu_layers=50,         # 50개 레이어를 GPU에서 처리
    context_length=512,
)
```

### gpu_layers 파라미터 이해
- 모델은 여러 개의 레이어(층)로 구성됨
- `gpu_layers=0`: 모든 레이어를 CPU에서 처리 (느림)
- `gpu_layers=50`: 50개 레이어를 GPU에서 처리 (빠름)
- `gpu_layers=-1` 또는 충분히 큰 수: 가능한 모든 레이어를 GPU로

### 실험 결과 (T4 GPU 기준 예상)
| 모드 | 응답 시간 |
|------|-----------|
| CPU | ~60초 |
| GPU | ~5초 |
| GPU 가속 | 약 10~15배 빠름 |

> **결론:** 로컬에서 LLM을 실용적으로 사용하려면 GPU가 사실상 필수.  
> CPU만 있다면 더 작은 모델(Q4, Q2 양자화)을 사용하거나 Ollama를 사용하는 것이 현실적.

---

## CHAPTER 6. Streamlit — Python으로 웹 UI 만들기

### Streamlit이란?
- Python 코드만으로 웹 앱을 만들 수 있는 프레임워크
- HTML/CSS/JavaScript 없이도 인터랙티브한 UI 구현 가능
- AI/데이터 사이언스 프로토타입에 특히 유용

```bash
pip install streamlit
streamlit run app.py    # 실행 → 자동으로 브라우저 열림
```

### 이 프로젝트에서 사용한 Streamlit 요소

```python
import streamlit as st

# 페이지 설정 (반드시 첫 번째로 호출)
st.set_page_config(
    page_title="이메일 생성기 📮",   # 브라우저 탭 제목
    page_icon='📮',                   # 탭 아이콘
    layout='centered',                # 레이아웃: 'centered' 또는 'wide'
    initial_sidebar_state='collapsed' # 사이드바 초기 상태
)

st.header("제목")                     # 큰 제목
st.selectbox("라벨", ["옵션1", "옵션2"])  # 드롭다운 선택
st.text_area("라벨", height=100)      # 여러 줄 입력창
st.columns([10, 10])                  # 화면 분할 (비율)
st.text_input("라벨")                 # 한 줄 입력창
st.button("버튼 텍스트")              # 버튼
st.spinner("로딩 중...")              # 로딩 애니메이션
st.write(결과)                        # 텍스트/데이터 출력
```

### Streamlit 작동 원리
```
사용자가 버튼 클릭
    ↓
스크립트 전체가 위에서 아래로 다시 실행됨
    ↓
변경된 UI만 화면에 업데이트
```
> 이 특성 때문에 `if submit:` 조건으로 버튼 클릭 여부를 확인해야 함.

---

## CHAPTER 7. LangChain — AI 연동을 쉽게

### LangChain이란?
- LLM(AI 모델)과 연동하는 작업을 표준화한 라이브러리
- 다양한 AI 모델을 동일한 방식으로 교체해서 사용 가능
- 프롬프트 관리, 체인 구성, 메모리, 에이전트 등 기능 제공

```bash
pip install langchain-core langchain-community langchain-ollama
```

### 패키지 구조 이해 (최신 버전 기준)
| 패키지 | 역할 |
|--------|------|
| `langchain-core` | 핵심 인터페이스 (PromptTemplate, OutputParser 등) |
| `langchain-community` | 서드파티 연동 (CTransformers 등) |
| `langchain-ollama` | Ollama 전용 연동 |
| `langchain-google-genai` | Google Gemini 전용 연동 |

### PromptTemplate 사용법
```python
from langchain_core.prompts import PromptTemplate

# 빈칸이 있는 템플릿 정의
template = """
{email_topic} 주제를 포함한 이메일을 작성해 주세요.
보낸 사람: {sender}
받는 사람: {recipient}
언어: {language}
"""

# PromptTemplate 객체 생성
prompt = PromptTemplate(
    input_variables=["email_topic", "sender", "recipient", "language"],
    template=template,
)

# 빈칸에 값 채우기
완성된_질문 = prompt.format(
    email_topic="회의 일정 안내",
    sender="김철수",
    recipient="이영희",
    language="한국어"
)
```

### LangChain 체인 방식 (파이프라인)
`Langchain/Poet/main.py`에서 사용한 더 현대적인 방식:

```python
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser

prompt = ChatPromptTemplate.from_messages([
    ("system", "You are a helpful assistant."),
    ("user", "{input}")
])

# 파이프(|)로 체인 연결: 프롬프트 → LLM → 출력 파서
chain = prompt | llm | StrOutputParser()

result = chain.invoke({"input": "시를 써줘"})
```

> **두 방식의 차이:**
> - `llm.invoke(prompt.format(...))` → 이메일 생성기 방식. 단순하고 직관적.
> - `chain = prompt | llm | parser` → 시인 앱 방식. 현대적이고 확장 쉬움.

---

## CHAPTER 8. Gemini API 연동 실습 — 인공지능 시인 앱

**파일:** `C:\Users\Jini Nam\Documents\GitHub\Langchain\Poet\main.py`

로컬 모델이 아닌 **Google Gemini API**를 사용한 앱.  
LangChain의 동일한 인터페이스로 클라우드 모델과 로컬 모델을 모두 다룰 수 있음을 확인.

```python
from langchain.chat_models import init_chat_model

# Gemini Flash 모델 초기화 (API 키 필요)
llm = init_chat_model(
    model="gemini-flash-latest",
    temperature=0.7,
    max_tokens=2048,
    model_provider="google_genai",
)
```

### 클라우드 LLM vs 로컬 LLM 비교
| 항목 | 클라우드 (Gemini/OpenAI) | 로컬 (Ollama/CTransformers) |
|------|--------------------------|------------------------------|
| 비용 | API 사용량만큼 과금 | 무료 |
| 속도 | 빠름 | 하드웨어에 따라 다름 |
| 개인정보 | 서버로 전송됨 | 내 PC에서만 처리 |
| 설치 | API 키만 필요 | 모델 파일 다운로드 필요 |
| 인터넷 | 필요 | 불필요 |

---

## CHAPTER 9. Ollama — 가장 쉬운 로컬 LLM 실행 방법

### CTransformers의 한계
- 모델 파일을 직접 다운로드해서 경로 지정해야 함
- 최신 모델 업데이트 관리가 번거로움
- 모델별로 설정 방법이 다름

### Ollama란?
- 로컬 LLM을 Docker처럼 손쉽게 관리하는 도구
- `ollama pull 모델명` 한 줄로 모델 다운로드
- 백그라운드에서 서버로 실행됨 → API로 접근

```bash
# 설치: https://ollama.com 에서 다운로드

# 모델 다운로드
ollama pull llama3          # Meta Llama 3 (4.7GB)
ollama pull llama3.1:8b     # Llama 3.1 8B
ollama pull mistral         # Mistral 7B

# 설치된 모델 목록 확인
ollama list

# 모델과 직접 대화 (터미널)
ollama run llama3

# 서버 실행 (백그라운드)
ollama serve
```

### 이 프로젝트의 설치 모델
```
NAME             ID              SIZE
llama3:latest    365c0bd3c000    4.7 GB
```

### Ollama + LangChain 연동
```python
from langchain_ollama.llms import OllamaLLM

# 모델 이름만 지정하면 끝 (파일 경로 불필요!)
llm = OllamaLLM(model="llama3:latest", temperature=0.7)

response = llm.invoke("이메일을 작성해주세요.")
```

### CTransformers vs Ollama 비교
| 항목 | CTransformers | Ollama |
|------|---------------|--------|
| 설정 난이도 | 높음 (파일 경로, 파라미터 직접 설정) | 낮음 (모델명만 지정) |
| 모델 관리 | 파일 직접 다운로드/삭제 | `pull`/`rm` 명령어 |
| 실행 방식 | Python 프로세스 내 직접 실행 | 별도 서버로 실행 후 API 호출 |
| 속도 | 약간 빠름 | 약간 느림 (네트워크 오버헤드) |
| 편의성 | 낮음 | 높음 |

---

## CHAPTER 10. 이메일 생성기 앱 개발 전 과정

### 앱 구조 설계
```
사용자 입력 (Streamlit UI)
    ↓
getLLMResponse() 함수 호출
    ↓
PromptTemplate으로 질문 완성
    ↓
OllamaLLM으로 AI에게 전송
    ↓
AI 답변을 화면에 출력
```

### 개발 단계별 변화

**1단계 (초기):** CTransformers + 로컬 .bin 파일
```python
from langchain_community.llms.ctransformers import CTransformers
llm = CTransformers(model='./llama-2-7b-chat.ggmlv3.q8_0.bin', model_type='llama')
```

**2단계 (전환):** Ollama로 교체
```python
from langchain_ollama.llms import OllamaLLM
llm = OllamaLLM(model="llama3.1:8b", temperature=0.7)
```

**3단계 (오류 수정 ①):** LangChain 버전 호환성 문제 해결
```python
# 오류: No module named 'langchain.prompts'
# 원인: LangChain 1.x에서 경로 변경됨

# 수정 전
from langchain.prompts import PromptTemplate

# 수정 후
from langchain_core.prompts import PromptTemplate
```

**4단계 (오류 수정 ②):** 설치된 모델명 불일치
```python
# 오류: model 'llama3.1:8b' not found (status code: 404)
# 원인: 설치된 모델은 llama3:latest, 코드는 llama3.1:8b

# 수정 전
llm = OllamaLLM(model="llama3.1:8b", temperature=0.7)

# 수정 후
llm = OllamaLLM(model="llama3:latest", temperature=0.7)
```

---

## CHAPTER 11. 환경 설정 및 패키지 관리

### Python 환경
- **설치 방식:** Miniconda
- **Python 경로:** `C:\Users\Jini Nam\miniconda3\python.exe`
- **Streamlit 경로:** `C:\Users\Jini Nam\miniconda3\Scripts\streamlit.exe`

### 설치된 주요 패키지
```bash
# 처음부터 있던 패키지
streamlit

# 이 프로젝트에서 추가 설치
pip install langchain langchain-community langchain-ollama
```

| 패키지 | 버전 | 역할 |
|--------|------|------|
| streamlit | - | 웹 UI |
| langchain-core | 1.4.8 | 프롬프트 템플릿 |
| langchain-community | 0.4.2 | CTransformers 연동 |
| langchain-ollama | 1.1.0 | Ollama 연동 |

---

## CHAPTER 12. 배포 — 바탕화면 바로가기

### 바탕화면 바로가기 생성 (PowerShell)
```powershell
$WshShell = New-Object -comObject WScript.Shell
$Shortcut = $WshShell.CreateShortcut("$env:USERPROFILE\Desktop\이메일 생성기.lnk")
$Shortcut.TargetPath = "C:\Users\Jini Nam\miniconda3\Scripts\streamlit.exe"
$Shortcut.Arguments = "run `"C:\Users\Jini Nam\Documents\GitHub\Multi lang email generator\app.py`""
$Shortcut.WorkingDirectory = "C:\...\Multi lang email generator"
$Shortcut.Save()
```

### 책 모양 아이콘 생성 (PowerShell + System.Drawing)
```powershell
Add-Type -AssemblyName System.Drawing

# 64x64 비트맵 생성
$bitmap = New-Object System.Drawing.Bitmap(64, 64)
$graphics = [System.Drawing.Graphics]::FromImage($bitmap)
$graphics.Clear([System.Drawing.Color]::Transparent)

# 책 이모지(📚) 렌더링
$font = New-Object System.Drawing.Font("Segoe UI Emoji", 44)
$brush = New-Object System.Drawing.SolidBrush([System.Drawing.Color]::Black)
$graphics.DrawString([char]::ConvertFromUtf32(0x1F4DA), $font, $brush, -4, 4)

# PNG → ICO 포맷 변환 후 저장
# (ICO 파일은 헤더 + PNG 바이트로 구성)
```

---

## 최종 파일 구조

```
Multi lang email generator\
├── app.py                          ← 메인 앱 (주석 포함)
├── email_icon.ico                  ← 책 모양 아이콘
├── log20260703.md                  ← 이 학습 로그
├── test_ctransformers_gpu.ipynb    ← GPU 속도 비교 실험 노트북
├── llama-2-7b-chat.ggmlv3.q8_0.bin ← Llama 2 GGML 모델 (7.16GB, 현재 미사용)
└── 이메일 생성기.lnk               ← 바탕화면 바로가기
```

---

## 앱 실행 방법

```bash
# 1. Ollama 서버 실행 (이미 실행 중이면 생략)
ollama serve

# 2. 앱 실행
streamlit run app.py

# 또는 바탕화면 "이메일 생성기" 아이콘 더블클릭
```

브라우저에서 http://localhost:8501 접속

---

## 핵심 개념 최종 정리

| 개념 | 한 줄 정의 |
|------|-----------|
| LLM | 대량 텍스트로 학습된 대형 언어 AI 모델 |
| Llama | Meta가 오픈소스로 공개한 LLM 시리즈 |
| GGML/GGUF | CPU에서 LLM을 실행하기 위한 경량 파일 포맷 |
| 양자화 | 모델 파일 크기를 줄이는 압축 기술 (Q8, Q4, Q2) |
| CTransformers | Python에서 GGML/GGUF 모델을 실행하는 라이브러리 |
| Ollama | 로컬 LLM을 쉽게 설치·실행하는 관리 도구 |
| Streamlit | Python으로 웹 UI를 만드는 프레임워크 |
| LangChain | 다양한 LLM과 연동을 표준화한 라이브러리 |
| PromptTemplate | AI에게 보낼 질문의 빈칸 채우기 양식 |
| temperature | AI 답변의 창의성 수치 (0=일정, 1=창의적) |

---

## 다음에 해볼 것들

- [ ] `ollama pull llama3.1:8b` 로 더 최신 모델 설치해서 비교
- [ ] Streamlit `st.session_state`로 대화 기록 유지
- [ ] 생성된 이메일 복사 버튼 추가
- [ ] 이메일 말투 선택 기능 추가 (격식체 / 비격식체)
- [ ] Groq API로 교체해서 웹 배포
